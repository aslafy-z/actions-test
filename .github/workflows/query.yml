on:
  workflow_dispatch:
    inputs:
      url:
        type: text
        description: 'URL'

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch URL
        uses: indiesdev/curl@v1.1
        with:
          log-response: true
          url: "${{ inputs.logLevel }}"
